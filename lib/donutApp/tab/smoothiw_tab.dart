import "package:flutter/material.dart";


class SmoothieTab extends StatelessWidget {
  const SmoothieTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child:Center(child: Text("Smoothiw TAB"),)
    );
  }
}