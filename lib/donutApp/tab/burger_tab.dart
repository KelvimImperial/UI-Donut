import "package:flutter/material.dart";


class BurgeTab extends StatelessWidget {
  const BurgeTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child:Center(child: Text("BurgerT TAB"),)
    );
  }
}